# 校园网一键登录（CampusLoginApp）

专为**成都信息工程大学**的锐捷认证体系做的校园网自动登录工具：填一次账号密码，点一下就能登录；还支持后台保活（掉线自动重连）和开机自动联网。**无需安装 Python，双击即用。**

> 仅支持本校（成都信息工程大学）的锐捷 ePortal 认证体系，其他学校 / 其他认证方式不适用。

---

## 📦 怎么发给同学使用（转发前必看）

> ⚠️ 顺序很重要：**先清理，再压缩，最后发送**。否则会把**你自己的密码**发出去！

**第一步（必须做）：删除你文件夹里的隐私文件**

打开 `CampusLoginApp` 文件夹，删除以下文件（有就删，没有跳过）：

- `config.json` —— **里面是你的账号密码，必须删！**
- `login.log` —— 运行日志
- `app.pid` —— 后台进程记录

**第二步：处理快捷方式**

- `校园网一键登录.lnk` 里写死了**你本机的绝对路径**，发给别人会失效，**建议删掉**（让对方直接双击 exe 即可）。

**第三步：压缩**

- 右键点击 `CampusLoginApp` 文件夹 → **「发送到 → 压缩(zipped)文件夹」**（或「压缩为 ZIP」），得到一个 `CampusLoginApp.zip`。

**第四步：发送**

- 通过微信 / QQ 把 `CampusLoginApp.zip` 发给同学。

**第五步：同学那边**

- 解压 zip → 双击 `CampusLogin.exe`；
- 若弹出蓝色 SmartScreen 警告，点 **「更多信息」→「仍要运行」**；
- 然后按下面的「三步用法」填写自己的账号密码即可。

> ❌ **反例**：不要把整个 `campus-login` 目录（含原始脚本和其他文件）打包发人！
> ✅ **只发 `CampusLoginApp` 这一个文件夹。**

---

## 一、首次使用

1. 双击 `CampusLogin.exe`（或双击 `校园网一键登录.lnk`）。
2. 如果 Windows 弹出蓝色 SmartScreen 警告（「Windows 已保护你的电脑」），这是**未签名 exe 的正常现象**：点 **「更多信息」** → **「仍要运行」**。
3. 等待程序窗口打开即可。

## 二、三步用法

### 第 1 步：登录

在「账号信息」卡片里填写**校园网账号**、**密码**，选择你的**运营商**（移动 / 电信 / 联通 / 教育网），点卡片底部的 **「保存并立即登录」**（配置文件里对应的字段名：cmcc / ctcc / unicom / local）：

- 显示「登录成功，设备已上线」或「已联网，无需登录」→ 可以上网了；
- 显示「本地网络不可达」→ 你现在没连上校园网，先连上网再点。

> **密码显示 / 隐藏**：密码框默认以圆点隐藏。点密码框右侧的 **「显示 / 隐藏」** 按钮可切换明文查看；也可以直接按快捷键 **Ctrl+H** 切换。切换只改变显示方式，不会改动你保存的密码。

### 第 2 步（可选）：后台保活 / 开机自动联网

这两件事是**互相独立**的，各管各的：

- **后台保活**（「保活与开机自启」卡片）——**现在这一刻**让后台服务跑起来：关闭程序窗口后它仍在后台跑，掉线自动重连。点「启动后台保活」开始，「停止后台保活」结束。
- **开机自启**（同一张卡下半张里的开关）——**以后每次**登录 Windows 时自动把后台服务拉起来，全程无弹窗、**不需要 UAC 授权**（开关拨动后 1 秒内完成）。

> 勾上「开机自启」**不会**改变后台保活现在的运行状态，点「启动保活」也**不会**动开机自启——一个是"现在跑"，一个是"以后自动跑"。

### 第 3 步（可选）：手机和电脑不抢线——自动让位

校园网账号通常**只允许一台设备在线**。寝室的电脑开着保活，你在教室用手机一认证，电脑就被踢下线；电脑保活立刻重连又把手机踢掉——两边反复互踢。程序会自动避免这件事：

- 掉线时先去自助服务查「**当前在线的是哪台设备**」（看 MAC 地址，跟本机网卡比对）：
  - 在线的是**手机 / 另一台电脑** → 确认是别的设备在用，**主动让位** 15 分钟（反复冲突则 30 / 60 / 120 分钟封顶），不跟你抢；
  - 没人在线、或在线的正是**本机** → 立刻正常登录；
  - 查不出来（自助服务不通）→ 退回老办法：按「刚认证成功没撑过 3 分钟就掉线」推断。
- 让位期间**每 2 分钟复查一次**：对方一下线，电脑马上自动重新上线，不用干等 15 分钟。
- 让位时界面上会显示一行提示（含对方设备类型），日志里也会写明让给了谁。

> 让位**只影响自动重新认证**，不会主动断开你当前的连接；你手动点「保存并立即登录」永远立刻生效。
> 查「谁在线」走的是自助服务（登录它不会占用校园网名额、不会把设备挤掉），并且**全程只读**——程序不会踢任何设备下线。
> 想查「现在谁在用」，命令行跑 `CampusLogin.exe --devices`。

### 第 4 步（可选）：检查有没有新版本

「设置」页底部「维护」卡里点 **检查更新**：程序会去 GitHub 上查有没有比当前更新的 Release，有就显示新版本号、发布日期和更新说明，问你要不要打开下载页面。

- **没联网或校园网未认证时**连不上更新服务器，这时会如实告诉你"未能连接到更新服务器"，不会假装"已是最新版本"。
- 程序**不会自己下载替换**（运行时 exe 被自己占用，覆盖必然失败），下载后请手动用新 exe 覆盖旧文件——`config.json` 里的账号密码不受影响。

### 第 5 步：不想用了

关掉「开机自启」开关 + 点「停止后台保活」，然后删除整个文件夹即可，不留任何残留。

---

## 三、界面速览

窗口左边是三个页面：**连接 / 日志 / 设置**，侧边栏底部常驻一个实时状态块（状态圆点 + 运营商 + 出口 IP）。

**「连接」页（平时只用这一页）**——版式是「上面两张卡 + 下面通栏日志」：

| 位置 | 卡片 | 作用 |
| --- | --- | --- |
| 顶部通栏 | 状态主卡（蓝色） | 连接状态 + 本机 MAC，右上是 IP 地址 / 运营商 |
| 上排左 | 账号信息 | 填账号 / 密码 / 运营商，卡片底部是「保存并立即登录」按钮和操作结果；密码框右侧「显示 / 隐藏」可切换明文（快捷键 Ctrl+H） |
| 上排右 | 保活与开机自启 | 启动/停止后台保活；保活状态行（让位时会多出一行只读提示，无按钮）；下半张卡是**开机自启**开关（与保活启停互相独立） |
| 下排通栏 | 运行日志 | 最近几条日志，点「查看全部」跳到「日志」页 |

> 上下拉伸窗口时，**只有最下面的日志区会变高变矮**（露出更多或更少的日志行），
> 上面的卡片高度不变。

**「日志」页** —— 完整的 `login.log`（最近 200 行），带刷新 / 打开日志目录。

**「设置」页** —— 保活参数（检测间隔 / 在线检测间隔 / 内容校验间隔 / 心跳日志间隔 / 退避上限）、高级参数（认证网关，可手动填写或点**自动探测网关**）、维护（检查更新 / 打开日志目录）、版本信息。这些都是改一次就不动的项，一般保持默认。

> ⚠️ **改完参数要记得点「保存参数」**。参数只存在界面上时不会生效——只有点了保存才会写入配置文件。后台保活运行中**不用重启**，它每轮都会重读配置，约一个检测间隔内自动生效。
>
> 各参数都标注了合法范围（例如检测间隔 5~86400 秒）。填了超出范围的值会被自动夹到边界，保存后输入框会刷新成**真正生效的值**，并在按钮下方说明改了哪几项。
>
> 「页面 ID customPageId」和「NAS IP nasIp」由认证网关下发，每次登录都会自动更新并保存，一般不用手动改（手动填的值只在网关没返回时才起作用）。

---

## 四、常见问题（FAQ）

- **显示「本地网络不可达」**：正常现象，表示当前没连上校园网（没插网线 / 没连校园 Wi-Fi / 刚开机还没连上）。连上校园网后自动恢复，无需处理。
- **登录页出现图形验证码**：验证码无法自动处理（锐捷默认通常没有验证码），需要去网页手动登录一次。
- **改了校园网密码**：打开程序填入新密码，重新点一次「保存并立即登录」；如果后台保活正在运行，按提示选择重启即可应用新配置。
- **登录失败 / 想排查问题**：看「连接」页的「运行日志」卡片，或翻「日志」页，也可以直接打开程序同目录下的 `login.log`（超 512KB 自动轮转）。
- **杀毒软件报毒 / SmartScreen 拦截**：程序未做数字签名，属误报；选择信任 / 仍要运行即可。
- **后台保活会不会很占资源**：不会。保活进程启动时会把自己设为「低于正常」优先级，尽量不与前台程序抢资源；联网稳定后检测间隔会自动放宽（默认 30 秒→60 秒），网页内容校验也会走缓存，因此每小时的后台请求与磁盘写入都很少。
- **手机和电脑互相把对方踢下线**：程序会自动处理，见「第 3 步：手机和电脑不抢线——自动让位」。判据首选**在线设备的 MAC**（自助服务能列出当前在线的是电脑还是手机），查不到时才按「认证成功后没撑过 3 分钟就掉线」推断；网络故障不会让网关返回认证成功，所以断网不会被误判成被抢占。想查「现在谁在用」可命令行跑 `CampusLogin.exe --devices`。若想完全关掉自动让位，把 `config.json` 里的 `yield_enabled` 改成 `false`；只想关掉 MAC 判定、保留时间推断，则把 `device_check_enabled` 改成 `false`。
- **换了一所学校还能用吗？**：能。每所学校的认证网关地址不同，程序可以自己找：「设置」→「高级参数」→ **自动探测网关**（需处于「连着校园网但还没认证」的状态，已能上网时拿不到跳转）。也可以在「认证网关」里手动填学校提供的 IP 或域名。登录时如果发现配置里的地址对不上，程序会自动改用实际探测到的地址并保存。
- **程序会自动生成哪些文件**：`config.json`（你的配置和密码）、`login.log`（日志）、`app.pid`（后台进程号），都在 exe 同目录，可随时删除（`config.json` 删了需重新填写）。

---

## 五、附：源码与重新打包

源码在 `src\` 文件夹：`app.py`（界面与入口）+ `login_core.py`（认证核心）+ `ui_render.py`（抗锯齿绘制原语）+ `build_assets.py`（图标颜色变体生成）+ `build.bat`（一键打包脚本）。

如需修改后重新打包：先安装**带 tkinter 的 Python 3.13+**，再双击 `src\build.bat` 即可（它会自动创建临时打包环境，不会污染系统 Python）。更完整的说明见同目录 **`打包分发指南.md`**。

### 发一个新版本（维护者看这里）

1. 改 `src\app.py` 里的 `APP_VERSION`（例如 `v2.2.0`），改完代码后重新打包；
2. 在 GitHub 仓库里 **Create a new release**：Tag 必须与 `APP_VERSION` **完全一致**（`v2.2.0`），说明写这次改了什么，附件上传 `CampusLogin.exe`；
3. 发布后，用户点「检查更新」就能查到。

> 仓库必须是 **Public**，否则用户端匿名请求读不到 Release（返回 404）。只发 Release 附件、不传源码即可，源码不会外泄。
> 版本号比较是按数字段做的，过 10 以后（如 `v2.10.0`）也不会被误判成比 `v2.9.0` 旧。

---

## 六、附：命令行用法（想用脚本 / 快捷方式自动化的同学）

不想每次点窗口，可以直接在命令行调用（在 exe 同目录打开终端）：

| 命令 | 作用 | 退出码 |
| --- | --- | --- |
| `CampusLogin.exe --start-keepalive` | 等价于点界面上的「启动后台保活」：拉起后台进程后**立即退出**（不驻留窗口） | 0=已拉起；1=未配置账号密码 |
| `CampusLogin.exe --stop-keepalive` | 等价于点「停止后台保活」 | 0=已停止或本就没在运行；1=停止失败 |
| `CampusLogin.exe --probe` | 诊断输出（网络状态 / AES 自检 / 网关跳转链），并顺带清理历史临时残留 | 恒为 0 |
| `CampusLogin.exe --once` | 检测并登录一次后退出（排错用） | 0=已联网；1=未联网 |

例如做一个「一键保活」快捷方式，目标填：
`"D:\first-cc\CampusLoginApp\CampusLogin.exe" --start-keepalive`

> 提示：这几个命令都是纯后台 / 诊断用途，不会弹出图形界面，也不会加载界面组件，因此占用极低。

---

> 「设置」页底部的「关于」卡片里永远提醒你：**转发本文件夹给他人前，请先删除 config.json（其中保存了你的密码）。**
